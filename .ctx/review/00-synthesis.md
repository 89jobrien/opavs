# MOA Review Synthesis — opavs (reviewed as full initial-commit codebase; no diff existed)

| Priority | Finding                                                                                                                                                                                                                                               | Lens          | File:Line                                                                        | Fix                                                                                                                                                                                          |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| HIGH     | Path convention mismatch between this crate (`.ctx/opavs/tasks.yaml`, `.ctx/opavs/phase`) and the still-installed shell plugin (`.ctx/GODMODE.tasks.yaml`, `.ctx/.opavs-phase`) — silent fail-open in both directions if wired in without reconciling | Blast radius  | `opavs-guard.sh:24`, `opavs-phase.sh:8` vs `src/repo.rs:8`, `src/adapters.rs:17` | Before wiring `opavs` binary into `hooks.json`: either update the shell scripts' paths, or fully swap `hooks.json` to call `opavs guard`/`opavs phase` and migrate existing scaffolded repos |
| HIGH     | `Command::Init` doc comment says it scaffolds `.ctx/GODMODE.tasks.yaml` — stale/wrong, real path is `.ctx/opavs/tasks.yaml`                                                                                                                           | Docs          | `src/main.rs:29`                                                                 | Fix doc comment to match `init.rs`/README/CLAUDE.md                                                                                                                                          |
| HIGH     | `run_guard()` has zero test coverage — the single most security-relevant function in the crate (the PreToolUse gate itself)                                                                                                                           | Test coverage | `src/main.rs:169`                                                                | Refactor to separate pure dispatch logic from `process::exit`, then add tests for Edit/Write/Bash paths, missing `tool_name`/`cwd`, unresolvable repo root                                   |
| HIGH     | No CLI-integration tests at all — every `Command`/`TasksAction` arm in `main.rs` (Init, Phase get/set, Tasks list/runnable/validate/set-status/import) is wired but untested                                                                          | Test coverage | `src/main.rs:83-221`                                                             | Add integration tests (via `assert_cmd` or direct function calls) covering each arm's happy + error path                                                                                     |
| MED      | `TasksAction::SetStatus`/`Validate`/`Import` error paths (invalid status, unknown id, validation failure post-merge) untested                                                                                                                         | Test coverage | `src/main.rs:121,130,147`                                                        | Add targeted unit/integration tests                                                                                                                                                          |
| MED      | `allow_and_exit()` calls `std::process::exit(0)` directly, making guard dispatch branches untestable without killing the test process                                                                                                                 | SOLID         | `src/main.rs:248`                                                                | Return a `Verdict`/enum from `run_guard`'s core logic; push `exit` to a thin `main()` wrapper                                                                                                |
| MED      | Mixed error-handling contract in `domain.rs`: `Phase::parse`/ports use `anyhow::Result`, `validate()` uses typed `GraphError`                                                                                                                         | API surface   | `src/domain.rs:15,41-44,76-79,99`                                                | Either adopt a typed `PhaseParseError` for symmetry, or accept the split as intentional (ports vs. pure validation) and document it                                                          |
| MED      | Public adapter/domain items (`FsPhaseStore`, `FsTaskStore`, trait methods, `Phase`, `Task`, `TaskGraph`, `GraphError`) have no doc comments                                                                                                           | Docs          | `src/adapters.rs`, `src/domain.rs`                                               | Add `///` docs, especially since these are the ports/adapters other code will depend on                                                                                                      |
| MED      | README doesn't document the guard's fail-open behavior when `.ctx/opavs/tasks.yaml` is missing (silently allows) — a real security property                                                                                                           | Docs          | `src/main.rs:218-221`, README.md                                                 | Add a line to README's guard section noting this                                                                                                                                             |
| LOW      | `command_touches_commit_or_push` is `pub` but only used internally (no `lib.rs`) — should be `pub(crate)`                                                                                                                                             | API surface   | `src/guard.rs:43`                                                                | Narrow visibility                                                                                                                                                                            |
| LOW      | Dead code: `re_words` computed then discarded via `let _ = re_words;`                                                                                                                                                                                 | SOLID         | `src/guard.rs:44`                                                                | Delete                                                                                                                                                                                       |
| LOW      | `merged.tasks.len() - base.tasks.len()` subtraction (business logic) lives in `main.rs` instead of `import.rs`                                                                                                                                        | SOLID         | `src/main.rs:152`                                                                | Move an `added_count` helper into `import.rs`                                                                                                                                                |
| LOW      | `guard::decide()` takes `repo_root: &str` while everything else uses `&Path`/`PathBuf`                                                                                                                                                                | API surface   | `src/guard.rs:12-17`                                                             | Take `&Path`, `.display()` at call site                                                                                                                                                      |
| LOW      | `PhaseStore::set`/`TaskStore::save` take `&self` not `&mut self`, forcing future non-FS adapters into interior mutability                                                                                                                             | API surface   | `src/domain.rs:41-44,76-79`                                                      | Consider `&mut self` if adding in-memory test adapters later                                                                                                                                 |
| LOW      | `Task` has all-public fields, no constructor — any future invariant requires a breaking change                                                                                                                                                        | API surface   | `src/domain.rs:56-62`                                                            | Not urgent pre-1.0; note for later                                                                                                                                                           |
| LOW      | `init::scaffold()` returns `Result<Vec<String>>` instead of `Result<Vec<PathBuf>>`, losing type info                                                                                                                                                  | API surface   | `src/init.rs:20`                                                                 | Consider `PathBuf` return type                                                                                                                                                               |
| LOW      | `TasksAction::Import` doc comment says "external GODMODE.tasks.yaml file" — could be misread as implying guard-path compatibility (it's schema-only)                                                                                                  | Docs          | `src/main.rs:65-67`                                                              | Clarify wording                                                                                                                                                                              |

## Notes

- No breaking-change/semver findings — this is the crate's first commit, nothing to break yet.
- `cargo check` and `cargo clippy --all-targets` both clean (confirmed by Lens 4).
- Hexagonal boundaries are respected throughout: domain stays I/O-free, adapters are the only filesystem touchpoints, `main.rs` is thin (aside from the two MED-severity SRP nits above).
